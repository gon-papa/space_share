type: object
properties:
  code:
    type: integer
    example: 400
  msg:
    type: string
    example: "Date must be between Jun 16, 1995 and Aug 06, 2024."
  service_version:
    type: string
    example: "v1"
